/**
 * Script that defines a constant variable and prints its value to the console.
 *
 * Constraints:
 * 1. Must use a constant variable called 'myVar'.
 * 2. Value must be "JavaScript is amazing".
 * 3. Must use console.log().
 * 4. Cannot use 'var'.
 */

// Create the constant variable 'myVar' with the required string value.
const myVar = "JavaScript is amazing";

// Print the value of the constant variable to the console.
console.log(myVar);
